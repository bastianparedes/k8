apiVersion: v1
kind: Service
metadata:
  name: back1-service
  namespace: default
spec:
  selector:
    app: back1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
