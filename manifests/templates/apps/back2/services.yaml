apiVersion: v1
kind: Service
metadata:
  name: back-2-service
  namespace: default
spec:
  selector:
    app: back-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
