apiVersion: v1
kind: Service
metadata:
  name: {{ include "chartss.fullname" . }}
  labels:
    {{- include "chartss.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chartss.selectorLabels" . | nindent 4 }}
